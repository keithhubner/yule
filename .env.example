# OpenAI API Configuration
# Get your API key from https://platform.openai.com/api-keys
OPENAI_API_KEY=your-api-key-here

# Optional: Specify which OpenAI model to use for error analysis
# Default: gpt-3.5-turbo
# OPENAI_MODEL=gpt-3.5-turbo

# Optional: Maximum file size in MB for uploaded archives
# Default: 100
# MAX_FILE_SIZE_MB=100

# Optional: Maximum number of days to look back in logs
# Default: 365
# MAX_DAYS_LOOKBACK=365

# Optional: Path to local logs directory
# When set, enables "Local Logs" mode in the UI for direct file access
# This is useful when running the container on the same server as Bitwarden
# Example: /logs (when mounting -v /opt/bitwarden/bwdata/logs:/logs:ro)
# LOCAL_LOGS_PATH=/logs
